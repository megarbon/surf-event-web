$(function () {
	$('.text').typed({
		strings: ['WEB DESIGNER', 'WEB DEVELOPER', 'DOG LOVER', 'COFFEE DRINKER'],
		typeSpeed: 50,
		backSpeed: 20,
		backDelay: 2000,
		showCursor: true,
		loop: true,
	});
});
